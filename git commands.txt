git add .
git commit -m "changes in java assesments, js, css designs"
git push origin main
cd .. -> go back directory


------ npm server starting (For Angular projects) -------
npm ng serve
ng serve
npm run
npm start
ng version

git remote -v

----for new projects angular------

install angular ->        npm install -g @angular/cli
creating new projrcts ->  ng new <project-name>
new component -> ng g c newComponent

---npm commands---

npm init --yes
npm install --save express -body-parser cors
// body parser is a middle ware to handle form data
// cors is a package to make requests among different ports